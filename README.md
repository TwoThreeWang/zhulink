# ZhuLink (竹林)

**ZhuLink** 是一个去算法推荐的内容聚合社区 (类似 Hacker News)。

> "在浩瀚的互联网中，信息如同散落的竹叶，繁多而难以捕捉。ZhuLink 意为将这些零散的信息汇聚成一片生机勃勃的竹林。"

它的核心理念是**依靠用户共识（投票）挑选有价值的内容，拒绝算法控制**，并提供清新护眼的“竹林”视觉体验。

## 🛠 技术栈 (Tech Stack)

本项目严格遵守以下更“回归本源”的技术选型：

*   **Backend**: Go (Golang), Gin, GORM (PostgreSQL)
*   **Frontend**: Go Templates (SSR), HTMX (交互), Alpine.js (轻量逻辑)
*   **Styling**: Tailwind CSS (Bamboo Theme)
*   **Markdown**: goldmark (解析) + bluemonday (安全过滤)
*   **Build**: Air (热重载), Makefile

## ✨ 功能特性 (Features)

*   **去算法排序**: 采用类似 Hacker News 的 `(P-1) / (T+2)^G` 时间衰减算法。
*   **无限层级评论**: 支持 Markdown 的递归树状评论区，支持折叠/展开。
*   **极简交互**: 使用 HTMX 实现无刷新点赞、删除和评论回复。
*   **竹林美学**: 精心调试的绿色护眼主题。
*   **硬核数据**: 数据库采用硬删除 (Hard Delete) 和级联删除 (Cascade)。
*   **安全**: 密码 Bcrypt 加密，Markdown XSS 过滤。

## 🚀 快速开始 (Quick Start)

### 前置要求
*   Go 1.21+
*   PostgreSQL
*   Node.js & NPM (仅用于构建 Tailwind CSS)

### 1. 克隆项目
```bash
git clone https://github.com/wangtwothree/zhulink.git
cd zhulink
```

### 2. 配置数据库与环境变量
本项目支持使用 `.env` 文件进行配置。
复制示例文件并修改：
```bash
cp .env.example .env
# 编辑 .env 文件，填入你的数据库连接串和随机 Secret
# vim .env
```
或者设置环境变量 `DATABASE_URL`。

### 3. 安装依赖
```bash
go mod tidy
npm install -D tailwindcss # 如果需要重新编译 CSS
```

### 4. 运行开发环境
使用 Make 命令一键启动 Air (后端热重载) 和 Tailwind (CSS 监听)：
```bash
make dev
```
访问: `http://localhost:8080`

### 5. 编译构建
```bash
make build
# 生成二进制文件 ./tmp/main
```

## 📂 目录结构

```text
zhulink/
├── cmd/server/       # 程序入口
├── internal/
│   ├── db/           # 数据库连接
│   ├── handlers/     # 业务逻辑 (Auth, Story, Vote)
│   ├── middleware/   # 中间件 (Auth)
│   ├── models/       # GORM 模型
│   └── utils/        # 工具库 (Hash, Markdown)
├── web/
│   ├── assets/       # Tailwind 输入文件
│   ├── static/       # 静态资源 (编译后的 CSS)
│   └── templates/    # HTML 模板
└── tailwind.config.js
```

## 📝 License
MIT
